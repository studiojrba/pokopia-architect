import { ComingSoon } from "./ComingSoon";

export function HousesTab() {
  return (
    <ComingSoon
      title="Houses"
      description="Houses detected in your scene. Assign up to 4 Pokemon per house (single habitat type), see live Comfy Level per Pokemon, and apply pre-built house-group rosters from the upstream planning docs."
    />
  );
}
